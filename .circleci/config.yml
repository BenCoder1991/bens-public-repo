version: 2.1

jobs:
  mytestjob:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run: return 1
      - run:
        name: what an error
        command: echo "complete failure"
        when: on_fail


workflows:
  my workflow:
    jobs:
      - mytestjob

‚