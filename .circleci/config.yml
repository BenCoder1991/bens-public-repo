version: 2.1
commands:
  print_pipeline_id:
    steps:
      - run: echo $CIRCLE_WORKFLOW_ID
jobs:
  mytestjob:
    steps:
      - print_pipeline_id


workflows:
  my workflow:
    jobs:
      - mytestjob

